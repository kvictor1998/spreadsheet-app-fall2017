let len = text => text.length;
let concatenate = (text1, text2) => text1 + text2;
/*let left = (text, numChars) => 
let right = (text, numChars) => 
let find = (findText, withinText, startNum) =>
let search = (findText, withinText, startNum) =>
let mid = (text, startNum, numChars) => 
let replace = (oldText, startNum, numChars, newChars) => 
let substituteOnce = (text, oldText, newText) =>*/ 